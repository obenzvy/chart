<%= form_for(@chart) do |f| %>
  <% if @chart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chart.errors.count, "error") %> prohibited this chart from being saved:</h2>

      <ul>
      <% @chart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :video_time %><br />
    <%= f.time_select :video_time %>
  </div>
  <div class="field">
    <%= f.label :comprehension %><br />
    <%= f.check_box :comprehension %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
